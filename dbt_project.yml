name: 'DVA_WGG'
version: '1.0.0'
config-version: 2

profile: 'DVA_WGG'

model-paths: ['models']
analysis-paths: ['analyses']
test-paths: ['tests']
seed-paths: ['seeds']
macro-paths: ['macros']
snapshot-paths: ['snapshots']

clean-targets:
  - 'target'
  - 'dbt_packages'

models:
  DVA_WGG:
    staging:
      +materialized: view
    raw_vault:
      +materialized: incremental

vars:
  datavault4dbt.ldts_alias: ldts
  datavault4dbt.rsrc_alias: rsrc
  batch_ldts: '2026-04-01 08:00:00'